[project]
name = "vevorweatherprom"
version = "0.0.1"
dependencies = [
    "flask",
    "pytz",
    "paho-mqtt",
    "requests",
    "dnspython",
    "prometheus-client>=0.22.1",
    "pydantic>=2.11.7",
]

[tool.uv]
package = false

[project.scripts]
vevorweatherprom = "vevorweatherprom.main:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/vevorWeatherProm"]